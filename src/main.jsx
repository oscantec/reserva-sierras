import React from 'react'
import ReactDOM from 'react-dom/client'
import { BrowserRouter } from 'react-router-dom'
import App from './App'
import './index.css'

// CRITICAL: Initialize configuration BEFORE React renders
// This ensures Google Sheets credentials are available immediately
import { initializeConfig } from './utils/config'
initializeConfig()

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <BrowserRouter>
            <App />
        </BrowserRouter>
    </React.StrictMode>
)
